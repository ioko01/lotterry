import React from "react";
import Reward from "../components/RewardComponents/Reward";

interface Props {}

const RewardPage = (props: Props) => {
    return <Reward />;
};

export default RewardPage;
